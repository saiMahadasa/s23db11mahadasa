extends layout

block content
  h1= title
  p Welcome to #{title}
  p hello i am sai subrahmanyam mahadasa
  button(type="button", style="background-color: gray; color: white; padding : 5px; border:none;" class="btn btn-primary" , onclick="goFaster()") Press Me
  div#faster Increase the speed


  block scripts
    script.
      var mph = 20;
      var change = 1;

      function goFaster() {
        mph += change;
        change += 1;
        document.getElementById("faster").textContent = `MPH ${mph} with change ${change}`;
      }
